package tss

// Write core function with TSS

// 1. get ECDSA public key from public keys of validators
// 2. verify ECDSA sig

func VerifyTSSSig(pubKey string, msg string, sig string) (bool, error) {
	return true, nil
}
