FOR SERVER: docker run -p 8080:9000 -v /data:/data incognitochain/incognito-logger server
            chmod 666 -R /data

FOR CLIENT: docker run -e server=<ip>:8080 -v /data:/data incognitochain/incognito-logger
